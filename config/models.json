{
  "$comment": "Routing chains per modality. The ORDER HERE IS AUTHORITATIVE — the router tries top to bottom and falls back on failure. Video is deliberately external-first (best quality if you bring a key) with the local FFmpeg floor last, so the agent works with zero keys and never dies when an external quota runs out.",
  "routing": {
    "llm": ["local:ollama"],
    "image": ["local:comfyui"],
    "stt": ["local:whisper"],
    "tts": ["local:xtts", "local:piper"],
    "video": ["local:comfy-i2v", "external:veo", "external:seedance", "external:ltx", "local:ffmpeg-anim"],
    "avatar": ["external:avatar-api"]
  }
}
